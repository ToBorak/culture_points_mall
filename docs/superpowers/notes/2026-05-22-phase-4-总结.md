# Phase 4 总结（2026-05-23）

## 已交付（21/21 tasks）

### Module A · 签到加分闭环（4.1-4.7）
- signin/domain：SigninCode + SigninRecord GORM 实体
- HMAC 双窗口校验（当前窗 60s + 上一窗 30s 容忍）
- Haversine GPS 围栏
- 全局题库（3 题 hardcode）
- service.Check 完整链路：HMAC → 重复签到检查 → GPS → 题 → 加分 → 徽章触发
- handler：POST /api/v1/signin/check + GET /admin/activities/:id/signin-code
- WebSocket /admin/activities/:id/signin-codes/stream（每 WindowSecs/2 推送新 code）
- H5 SigninPage（GPS → 答题 → 提交）
- admin 二维码大屏（WebSocket + QRCodeCanvas）
- 真 MySQL dockertest 集成测试

### Module B · 商城与盲盒 TCC（4.8-4.13）
- mall/domain：Item / BlindboxPrize / Freeze（try/confirmed/cancelled 状态机）/ Order
- points/service 新增 TCC 原语：TryFreeze(Redis SetNX) / Confirm(DB 事务扣分) / CancelByTxID(Redis Del)
- mall.Draw 完整 TCC 链路：余额检查 → Redis 占位 → freeze 行 → weighted pick → win/miss 分支
- 「未中奖」识别（prize_name 含「未中奖」/「鼓励」/「差一点」）→ Cancel
- 中奖 → Confirm + Order 落账，扣分绑定到租户第一个维度
- FreezeSweeper 兜底 worker（5s 扫描过期 freeze 强制 Cancel）
- TCC 真 MySQL + miniredis 集成测试

### Module C · H5 盲盒抽奖 UI（4.14-4.16）
- BlindboxWheel（@react-three/fiber 3D 圆盘 + 物理减速）
- MallPage 商品列表 + BlindboxDrawPage 抽奖页
- Lottie 金粉中奖动画（占位资源，待替换）

### Module D · 收尾（4.17-4.21）
- seed 增量：50 员工 × 3~8 条流水演示数据
- Playwright E2E 框架（3 个 spec：passport / agent-chat / blindbox）
- 双仓 README
- 验收记录
- Phase 4 + 整体收尾总结

## 实施过程中的工程修复

- `RequestID json.RawMessage` 需要显式 `UnmarshalJSON` / `MarshalJSON`（Phase 3 修过 + Phase 4 受益）
- `achievements.AwardBadge` 公开方法转发（Phase 3 修过）
- mall 抽奖扣减需绑定一个真实维度，否则 dim_id=0 写入 point_transactions 不合理 → `resolveConsumptionDim` 取租户第一个维度
- BlindboxWheel useMemo 应改 useEffect（副作用而非派生值）
- mall handler 返回 GORM struct，前端用 PascalCase 字段名匹配（短期方案）

## 已知局限

- signin 题库 hardcode 3 题，未抽 quiz_pool 表
- 盲盒扣减绑定到「第一个维度」而非租户配置的「消费默认维度」
- Lottie 资源为占位 JSON（空 layers），真演示需替换 lottiefiles 金粉动画
- MCP Server 鉴权 demo（任意非空 token）
- 历史会话点击切换未完全实现（SessionsSidebar 仅展示）

## 累计 commit 数

后端 70+ commit · 前端 30+ commit（以 git log 实际为准）
